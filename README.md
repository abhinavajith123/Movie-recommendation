# Movie-recommendation
This project implements a simple movie recommendation system using  PyTorch embeddings. It predicts how much a user will rate a movie based on past ratings (collaborative filtering).


At first run the predict.py file to create the model,then execute the recommendation.py to get the recommendation
